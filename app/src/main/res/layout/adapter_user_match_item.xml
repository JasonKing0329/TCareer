<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="bean"
            type="com.king.app.tcareer.page.match.gallery.UserMatchBean"/>
    </data>
    <RelativeLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/match_group"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/swipecard_bg"
            android:orientation="vertical" >

            <ImageView
                android:id="@+id/iv_img"
                android:layout_width="300dp"
                android:layout_height="180dp"
                android:layout_marginTop="20dp"
                android:scaleType="fitXY"
                app:matchUrl="@{bean.imageUrl}"/>

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="150dp"
                android:layout_marginTop="15dp"
                android:padding="12dp" >

                <TextView
                    android:id="@+id/tv_level"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:maxLines="1"
                    android:text="@{bean.nameBean.matchBean.level}"
                    android:textColor="@color/swipecard_text_normal"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/tv_court"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_toRightOf="@id/tv_level"
                    android:maxLines="1"
                    android:layout_marginLeft="25dp"
                    android:text="@{bean.nameBean.matchBean.court}"
                    android:textColor="@color/swipecard_text_hard"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/tv_num"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/tv_court"
                    android:layout_marginTop="8dp"
                    android:maxLines="1"
                    android:textColor="@color/swipecard_text_normal"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/tv_best"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/tv_num"
                    android:textColor="@color/swipecard_text_normal"
                    android:minLines="2"
                    android:textSize="14sp" />

            </RelativeLayout>

        </LinearLayout>
    </RelativeLayout>
</layout>